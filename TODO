- repackage for multilib
- Support for other technologies (I have no idea if these are possible, I'm just randomly thinking these up)
  - Supermon
  - Clumon
  - Nagios
  - fping?
  - linux-ha
- Need to rename project?
  - whazzup
- Symbol issloading uew wih Nodeupdown
- mechanism to determine time of last state change
- cleanup/fix conffile lib
  - list lens should be unsigned
  - uints instead of just ints
- conffile
  - use file streams (i.e. fread) to avoid constant syscalls to read()
    when reading a line.
